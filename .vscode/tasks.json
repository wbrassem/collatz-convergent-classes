{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate All Figures",
            "type": "shell",
            "command": "python",
            "args": [
                "python/generate_all_figures.py"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}
